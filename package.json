{
  "name": "sindoor-daan",
  "version": "1.0.0",
  "description": "Sindoor Daan - India's Relationship Verification Platform",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "start-backend": "cd backend && npm run dev",
    "start-frontend": "cd frontend && npm start"
  }
}
